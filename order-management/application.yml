server:
  port: 0
  servlet:
    contextPath: /order-management
spring:
  application:
    name: order-management
  datasource:
    url: jdbc:postgresql://localhost:5432/Amzur
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: 
    password: 
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
